// import { TestSchemaType } from "../schema/test.schema";

// export function testDto(response: TestSchemaType): ProductListEntity {
//   return new TestListEntity(
//     response.map(
//       (product) => new ProductEntity(product.id, product.title, product.price)
//     )
//   );
// }
